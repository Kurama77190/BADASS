
FROM alpine:latest

# Install necessary packages
RUN apk update && apk add --no-cache busybox iproute2 iputils bash nano

# Ajout du script de configuration VXLAN
COPY setup-vxlan.sh /setup-vxlan.sh
RUN chmod +x /setup-vxlan.sh

# Exécuter le script au démarrage
CMD ["../scripts/vxlan.sh"]
